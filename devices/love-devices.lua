return {
	system = require("devices.system"),
	console = require("devices.console"),
	screen = require("devices.screen"),
	controller = require("devices.controller"),
	mouse = require("devices.mouse"),
	file = require("devices.file"),
	datetime = require("devices.datetime"),
}
